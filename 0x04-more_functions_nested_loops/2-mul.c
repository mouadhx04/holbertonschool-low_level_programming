#include "holberton.h"
/**
 * mul - multiplies two integers
 *@a: The character to multiplie
 *@b: the character to multilpie
 * Return: 0 (success)
 */
int mul(int a, int b)
{
int x;
	if (a != '0' && b != '0')
		x = a * b;
		return (x);
}
