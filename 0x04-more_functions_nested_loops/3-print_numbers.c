#include "holberton.h"
/**
 * print_numbers - prints the numbers, from 0 to 9
 *
 *
 * Return: 0 (success)
 */
void print_numbers(void)
{
	if (c > '0' && c < '9')
	{
		_putchar(c + '0');
	}
	_putchar('\n');
	return (0);
}
