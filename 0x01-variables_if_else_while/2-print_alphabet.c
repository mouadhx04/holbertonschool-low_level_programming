#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char alpha = 'a';
if (alpha <= 'z')
{
putchar("alphabet in lowercase\n", alpha);
}
return (0);
}
