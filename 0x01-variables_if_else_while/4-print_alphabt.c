#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char i;
while (i== 'a' && i <= 'z' != 'q' && i != 'e')
{
putchar(i);
}
putchar('\n');
return(0);
}

