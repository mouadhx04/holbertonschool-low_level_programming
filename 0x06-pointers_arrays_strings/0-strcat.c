#include "holberton.h"
/**
 *_strcat - concatenates two strings
 *@dest: char pointer
 *@src: char pointer
 *Return: dest (Success)
 */
char *_strcat(char *dest, char *src)
{
	char *dest = str1;
	char *src = str2;

	while(*(++dest));
	while(*(dest) = *(src));
	*dest = '\0';
	return (dest);
}
