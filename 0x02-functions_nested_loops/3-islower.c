#include "holberton.h"

/**
 * _islower - checks for lowercase character
 *
 * Returns 1 if c is lowercase
 *
 * Returns 0 otherwise
 */
int _islower(int c)
{
int c;
if (c >= 'a' && c <= 'z')
_islower = 1;
return (0);
}
