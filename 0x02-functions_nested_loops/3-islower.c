#include "holberton.h"

/**
 * _islower - checks for lowercase character
 *
 * Returns 1 if c is lowercase
 *
 * Returns 0 otherwise
 */
int _islower(void)
{
int x;
if (x >= 'a' && x <= 'z')
_islower = 1;
return (0);
}
