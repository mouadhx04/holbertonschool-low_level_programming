#include "holberton.h"

/**
 * _islower - checks for lowercase character
 *@x: The character to check
 *
 * Return: 1 (Success)
 */
int _islower(int c)
{
if (x < 'z' && x > 'a')
return (1);
else
return (0);
}
