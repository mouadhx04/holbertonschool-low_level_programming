#include <stdio.h>
/**
 * main - prints sum of multiples of 3 and 5
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x = 0;
	int y = 1;
	int s;

	while (s < 50)
		s = x + y;
	printf("%d", s);
	printf(",");
	printf(" ");
	return (0);
}
